# demo

A static HTML/CSS/JS learning sandbox for exploring general web fundamentals. No build tools or dependencies — just open `index.html` directly in a browser.

## Structure

```
index.html   entry point
css/         stylesheets
js/          scripts
assets/      images, fonts, etc.
```

## Running

Open `index.html` in your browser. No server or build step required.
